Always run type checking when adding code, and add appropriate tests for project changes.

Spec Kit is installed for Codex as project skills under `.agents/skills`. In Codex, use
`$speckit-<command>` names such as `$speckit-specify`, `$speckit-plan`,
`$speckit-tasks`, and `$speckit-implement`. If a user types a Spec Kit command in
slash-command form, such as `/speckit.specify` or `/speckit-plan`, interpret it as
the matching `$speckit-*` skill request when the message reaches the model.

<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan
<!-- SPECKIT END -->
